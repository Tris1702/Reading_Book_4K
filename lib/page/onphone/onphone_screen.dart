import 'package:flutter/material.dart';

class OnphoneScreen extends StatelessWidget {
  const OnphoneScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Text('on pphone');
  }
}
